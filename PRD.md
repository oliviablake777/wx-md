# PRD：通用微信公众号 Markdown 渲染组件（wx-md）

> 阶段一：需求文档。确认后进入原型与实现阶段。

## 1. 背景与问题

微信公众号后台的图文编辑器：
- 不支持 Markdown 语法，只能手动排版，效率低；
- 富文本样式能力弱，且会**剥离 `class`、`<style>` 标签和外部 CSS**，只保留内联样式；
- 多平台发文时，内容需要重新排版，成本高。

## 2. 产品定位

一个**通用、可复用**的 npm 渲染组件：

- 输入：Markdown 源文本
- 输出：**全部样式内联化**的 HTML（微信编辑器可直接识别的格式）
- 能力：一键复制富文本，粘贴进微信公众号编辑器即得成品排版

### 2.1 形态

| 形态 | 说明 | 阶段 |
|---|---|---|
| 纯渲染库（核心交付） | 跨框架 JS/TS 库，任意项目（Vue / React / 原生）可直接引入 | MVP |
| Web 组件封装 | 封装为 `<wx-md>` 自定义元素，HTML 直接写标签 | P1 |
| 在线工具页 | 左侧写 Markdown、右侧实时预览、一键复制 | P2 |

### 2.2 运行环境

- 浏览器（主要，复制行为依赖浏览器 API）
- Node / SSR 安全（服务端可渲染，无 `window` 强依赖）

## 3. 目标用户

- 公众号运营者 / 内容创作者（排版工具使用者）
- 前端开发者（将 Markdown 渲染能力集成进自己的 CMS / 博客 / 内容平台）
- 需要批量格式化公众号文章的平台方

## 4. 核心功能需求

### 4.1 Markdown 渲染

- 支持 CommonMark 全量语法 + GFM 扩展：
  标题、段落、粗体/斜体、链接、图片、有序/无序列表、引用块、行内代码、代码块、
  表格、删除线、任务列表、水平线、脚注、自动链接
- 输出结构干净、语义化的 HTML 标签

### 4.2 微信适配（关键差异化，必须）

微信编辑器会剥离外部 CSS 与 `class`，因此：

- **所有样式内联化**：渲染结果中每个元素携带 `style="..."`，无 `<style>` / `class` / `id` 依赖
- 遵循微信排版约束：
  - 正文字号范围（推荐 14–16px，最大不超过 20px）
  - 合理行高（1.6–1.75）与段间距
  - 图片 `max-width: 100%`，自动适配手机宽度
  - 表格自适应宽度、代码块横向滚动
  - 页面整体宽度按微信阅读宽度约束
- 链接样式内联（颜色、下划线），避免被微信默认样式覆盖

### 4.3 一键复制

- **复制富文本（主路径）**：通过 Clipboard API 复制 `text/html`，用户直接在微信编辑器 Ctrl/Cmd+V，所见即所得
- **复制 HTML 源码（备选）**：复制完整 HTML 代码，可粘贴到微信编辑器右上角「HTML 模式」
- 浏览器兼容降级方案（老浏览器回退）

### 4.4 主题系统

- 内置多套主题：默认、极简、深色、学术（至少 2 套进 MVP）
- 主题维度：正文/标题颜色、字体、行高、列表/引用样式、代码块配色、表格样式、图片圆角与边框
- 支持自定义主题：通过配置对象定义

### 4.5 扩展能力（按阶段交付）

| 能力 | 说明 | 阶段 |
|---|---|---|
| 代码块语法高亮 | 颜色全部内联（prism 主题转内联色板） | MVP |
| 数学公式 | KaTeX 渲染 LaTeX 公式为可显示 HTML | P1 |
| 图片处理 | 尺寸/圆角/边框样式；本地图片转 base64 内嵌 | P1 |
| 目录 TOC / 锚点 | 长文导航 | P2 |
| 脚注 | GFM 脚注支持 | P1 |

## 5. API 设计草案

```ts
import { WxMd } from 'wx-md';

// 基础用法
const wxMd = new WxMd({ theme: 'default' });
const result = wxMd.render('# 标题\n\n正文内容');

// result.html    → 内联样式的 HTML（粘贴到微信 HTML 模式）
// result.preview → 可在页面展示的 HTML（用于自身预览）

// 一键复制富文本（浏览器环境）
await wxMd.copy(result.html);        // Ctrl/Cmd+V 直接粘贴到微信编辑器
await wxMd.copyHtml(result.html);    // 复制 HTML 源码
```

设计原则：
- 零依赖或极简依赖，体积可控
- API 简洁，渲染与复制解耦（复制行为仅浏览器端提供）
- 支持 `markdown-it` 插件式扩展入口

## 6. 非功能需求

- **兼容性**：现代浏览器（Chrome / Edge / Safari / Firefox 近两年版本）+ SSR 安全
- **体积**：核心渲染产物尽可能小（目标 gzip 后 < 60KB，含内联主题）
- **质量**：单元测试覆盖核心语法与内联样式断言；提供 playground 示例页
- **文档**：README 含快速上手、API 说明、主题定制指南；生成 TypeDoc
- **许可**：沿用仓库现有 LICENSE（MIT）

## 7. MVP 范围（P0）

1. Markdown → 内联样式 HTML 渲染（CommonMark + GFM）
2. 微信排版约束适配（字号、行高、图片宽度、代码块）
3. 一键复制富文本 + 复制 HTML 源码
4. 内置 2 套主题 + 自定义主题接口
5. 代码块语法高亮（内联色板）
6. 示例 playground 页 + 单元测试

P1：数学公式、图片处理、脚注、Web 组件封装、更多主题
P2：在线工具页（编辑器 + 实时预览）、目录 TOC、批量转换

## 8. 验收标准

- 将标准 Markdown 示例渲染后粘贴进微信编辑器，样式完整、无需二次调整
- 渲染产物中不存在 `class` / `<style>` / 外部样式依赖
- 在 Vue、React、原生 HTML 三种场景均可直接使用
- 复制富文本在主流浏览器中均可成功粘贴到微信编辑器
- 全部用例通过自动化测试，无控制台报错

## 9. 竞品参考（用于定位差异）

- doocs/md、md2wechat、wechat-mdeditor：均为**在线工具站**形态，代码库与业务耦合，难以复用
- 本组件差异化：以 **npm 渲染库**为核心，可被任意项目集成，同时保留后续工具站形态

## 10. 阶段划分（每阶段完成后确认再继续）

- 阶段一：本需求文档 → **待确认**
- 阶段二：工程初始化 + 核心渲染与内联样式
- 阶段三：主题系统 + 一键复制
- 阶段四：代码高亮 + 测试 + playground
- 阶段五：打包发布 npm + 文档完善
